This is a simple application that covers the scope of bundling and caching with webpack

## Getting Started

First, run the development server:

`npm run dev`


For an optimized production build, run 

`npm run build`